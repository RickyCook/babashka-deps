function mac() {
    requires homebrew
    requires homebrew_up_to_date
    requires git
    requires zsh
    requires vim
    requires packages
    requires casks
    requires spacemacs
    requires op_cli
    requires helmfile
}
